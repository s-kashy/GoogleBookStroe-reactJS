import React from "react";
import classes from "./Tilte.css";
const toolBar = props => {
  return (
    <div>
      <p className={classes.Title}>My Favourite Food Books</p>
    </div>
  );
};

export default toolBar;
